apiVersion: v1
kind: Service
metadata:
  name: {{ include "tutor-service.fullname" . }}
  labels:
    {{- include "tutor-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tutor-service.selectorLabels" . | nindent 4 }}
