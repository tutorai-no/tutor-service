apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tutor-service.fullname" . }}-test-connection"
  labels:
    {{- include "tutor-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tutor-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
